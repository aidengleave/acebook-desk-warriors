language:
 — ruby
rvm:
 — 2.5.0
cache:
 — bundler
script:
 — bundle install
 — bin/rails db:create
 — bin/rails db:migrate
 — bundle exec rspec
services:
 — postgresql

deploy:
  provider: heroku
 api_key:
  secure: "18bcf4e8-e993-4fbb-961b-ad77b22c3ed9"
app: acebook-team-desk-warriors
  on:
 repo: https://github.com/aidengleave/acebook-desk-warriors
